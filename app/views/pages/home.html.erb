<div class="homepage text-center container-fluid">
    <h1>My sample homepage!</h1>
    <h3>I'm learning Ruby-on-Rails right now and trying to build a demo SaaS web application.</h3>
</div>

<div class="container home-signup-links">
    <div class="row">
        
        <% if user_signed_in? %>
            <div class="col-md-6">
                <div class="well">
                    <h2 class="text-center">Your Profile</h2>
                    <% if current_user.profile %>
                        <%= link_to "Edit your profile", edit_user_profile_path(user_id: current_user.id), class: "btn btn-default btn-lg btn-block" %>
                        <%= link_to "View your profile", user_path(id: current_user.id), class: "btn btn-default btn-lg btn-block" %>
                    <% else %>
                        <p>Create your profile so that you can share your information with the community!</p>
                        <%= link_to "Create your profile", new_user_profile_path(user_id: current_user.id), class: "btn btn-default btn-lg btn-block" %>
                    <% end %>
                </div> 
            </div>
            <div class="col-md-6">
                <div class="well">
                    <h2 class="text-center">The Community</h2>
                    <p>Meet and see other pets and aspiring pet owners!</p>
                    <%= link_to "Visit the community", users_path, class: "btn btn-default btn-lg btn-block" %>
                </div> 
            </div>
            
        <% else %>
            <div class="col-md-6">
               <div class="well">
                   <h2 class="text-center">Basic Membership</h2>
                   <h4>Sign up for free and get basic access to the community.</h4>
                   <br>
                   <%= link_to "Sign Up (Basic)", new_user_registration_path(plan: @basic_plan.id), class: "btn btn-primary btn-lg btn-block" %>
               </div> 
            </div>
            <div class="col-md-6">
                <div class="well">
                    <h2 class="text-center">Pro Membership</h2>
                    <h4>Sign up for a pro account for only $7.77/month and get access to special content!</h4>
                    <%= link_to "Sign Up (Pro)", new_user_registration_path(plan: @pro_plan.id), class: "btn btn-success btn-lg btn-block" %>
                </div>
            </div>
        <% end %>
    </div>
</div>