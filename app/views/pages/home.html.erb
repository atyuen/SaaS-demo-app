<body class="homepage">
    <div class="jumbotron container text-center text-block">
        <h1>Welcome to DevNation!</h1>
        <h3 class="text-danger">
            DevNation is a fictional startup where developers and entrepreneurs can connect with each other. <br>
            I'm teaching myself Ruby-on-Rails and this is my demo SaaS web-application. When signing up for a 
            'Pro' account, just enter fake card information (any random 16-digit number).
        </h3>
    </div>
    <div class="container home-signup-links">
        <div class="row">
            <% if user_signed_in? %>
                <div class="col-md-6">
                    <div class="well text-block">
                        <h2 class="text-center">Your Profile</h2>
                        <% if current_user.profile %>
                            <%= link_to "Edit Profile", edit_user_profile_path(user_id: current_user.id), class: "btn btn-primary btn-lg btn-block" %>
                            <%= link_to "View Profile", user_path(id: current_user.id), class: "btn btn-primary btn-lg btn-block" %>
                        <% else %>
                            <p>Create your profile so that you can share your information with the community!</p>
                            <%= link_to "Create Profile", new_user_profile_path(user_id: current_user.id), class: "btn btn-default btn-lg btn-block" %>
                        <% end %>
                    </div> 
                </div>
                <div class="col-md-6">
                    <div class="well text-block">
                        <h2 class="text-center">The Community</h2>
                        <p>Enter the community and meet other developers, entrepreneurs, and investors! Only 'Pro' members will be able to contact other users.</p>
                        <%= link_to "Visit Community", users_path, class: "btn btn-success btn-lg btn-block" %>
                    </div> 
                </div>
            <% else %>
                <div class="col-md-6">
                   <div class="well text-block">
                       <h2 class="text-center">Basic Membership</h2>
                       <h4>Sign up for free and get basic access to the community.</h4>
                       <br>
                       <%= link_to "Sign Up (Basic)", new_user_registration_path(plan: @basic_plan.id), class: "btn btn-primary btn-lg btn-block" %>
                   </div> 
                </div>
                <div class="col-md-6">
                    <div class="well text-block">
                        <h2 class="text-center">Pro Membership</h2>
                        <h4>Sign up for a pro account for only $7.77/month and get access to special content!</h4>
                        <%= link_to "Sign Up (Pro)", new_user_registration_path(plan: @pro_plan.id), class: "btn btn-success btn-lg btn-block" %>
                    </div>
                </div>
            <% end %>
        </div>
    </div>
</body>